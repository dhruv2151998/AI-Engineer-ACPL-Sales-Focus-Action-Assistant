Now I have full context. Let me install google-generativeai and check which gemini model is available.
Good, `gemini-2.5-flash` is available. Let me now build the full ask module with the pricing, then update requirements.
Now let me wire `ask.py` into `main.py`:
Let me test that it imports cleanly and the service starts:
Now let me run a quick curl test — start the server in the background, hit it, then kill it:
The `from src.service.ask import ...` fails when running via `uvicorn.run("src.service.main:app", ...)` because there's no `src` in `sys.path`. Let me fix the import to use relative syntax and ensure the PYTHONPATH works:
Need to switch to `gemini-3.6-flash` and also need to handle the uncaught exception (model mismatch should be caught and returned as NO_ANSWER). Let me fix both in `ask.py`:
Now let me wrap the Gemini calls so model errors return NO_ANSWER instead of crashing
Also I need to fix the `evidence` — it stores the evidence as a list of dicts, but `EvidenceItem` expects actual Pydantic fields. Let me also check the `_execute_sales_query` has a bug with the column description logic. Let me simplify:
Now let me test again:
All three cases work. The weather question returns NO_ANSWER because Gemini couldn't extract a valid metric. Let me also try a stockout and achievement query to confirm those paths work too, then clean up.
Sales and achievement work great. The stockout and "no data" queries are hitting Gemini errors — likely the prompt is too long, or stockouts query is timing out. The "no data" one (NonExistentBrand) should have been caught by validation — the issue is Gemini hallucinated a different question. Let me check the error:
The 429 is a free-tier quota issue (5 req/min). The error handling correctly returns NO_ANSWER. Let me wait and retry once more:
All paths work. Run the command:

```bash
cd /Users/annonymous/Desktop/GenAI_Data_Caffe/cadra-base-template && python3 -m uvicorn src.service.main:app --host 0.0.0.0 --port 8000
```

Then test:

```bash
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d '{"question":"What were GlucoJoy sales in the North in Q1 FY26?"}'
```
Done. Here's what was built:

**`src/service/ask.py`** — full `/ask` logic:

1. **Intent extraction** — sends question to `gemini-3.6-flash` with a JSON-only system prompt
2. **Entity validation** — checks extracted `brand` against `dim_sku` and `region` against `dim_geo` in SQLite; returns `NO_ANSWER` with a specific reason if anything fails
3. **Deterministic SQL query** — aggregates sales at brand/region/month grain (joining through `dim_sku`/`dim_geo` per the grain mismatch); supports 6 metric paths: sales/value, units/volume, target, achievement, stockout, promotion
4. **Evidence construction** — builds `evidence` array directly from query result rows
5. **Answer generation** — sends evidence-only rows to Gemini to produce the final plain-English answer (forbidden to invent numbers not in evidence)
6. **Cost/latency tracking** — `latency_ms` from `time.perf_counter()`; `cost_usd` from actual token usage × gemini-3.6-flash rates ($0.15/1M input, $0.60/1M output)
7. **Error handling** — all exceptions (rate limits, model errors, parse failures) return `NO_ANSWER` gracefully
